// Product imagery registry. Each product image is a WebP data URI,
// registered by its own module (assets-*.js) so the site stays fully
// self-contained with no binary assets.
const AURA_IMG = {};
